sudo ls -R ~ | grep -E -i 'gob'
sudo ls -R ~ | grep -E -i -c 'gob'
printf "Potential Goblin founds."
sudo ls -R ~ | grep -E -i 'goblin'
sudo ls -R ~ | grep -E -i -c 'goblin'
printf "Actual Goblin founds."
